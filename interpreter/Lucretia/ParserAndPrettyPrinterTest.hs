module ParserAndPrettyPrinterTest where

{-TODO

Lu pretty printer
	test
		firstIteration = pretty (parser sourceCode)
		secondIteration = pretty (parser firstIteration)
		if (firstIteration == secondIteration) {}
		else
			diff
-}
